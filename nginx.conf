events {}

http {
    server {
        listen 80;
        server_name demo.saiteja.teja;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
